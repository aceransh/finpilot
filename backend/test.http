### whoami with explicit demo user (should echo that uid)
GET http://localhost:8080/api/v1/auth/whoami
Accept: application/json
X-Demo-User: alice

### whoami without header (should fall back to demo uid if allow-demo=true)
GET http://localhost:8080/api/v1/auth/whoami
Accept: application/json